# dining

Standalone Next.js service for UMD dining digest APIs + cron.

## TEMP UI NOTE
The root page is intentionally temporary and not a design reference.

## Run
npm install
npm run dev

## Endpoints
- /api/dining/health
- /api/dining/run
